import { ShowQuantityPipe } from './show-quantity.pipe';

describe('ShowQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
